"""Test suite for the Your First Light API.

Covers unit tests (parsers, compute helpers), integration tests
(individual response categories), end-to-end request lifecycle,
and rate-limiting behaviour.
"""
